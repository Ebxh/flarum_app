name: core
description: A flarum application by Flutter.

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.16.1
  shared_preferences: ^0.5.7+3
  dio: ^3.0.9
  cached_network_image: ^2.2.0+1
  path_provider: ^1.6.11
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
flutter_intl:
  enabled: true
